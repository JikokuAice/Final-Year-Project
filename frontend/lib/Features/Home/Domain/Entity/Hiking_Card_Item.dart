class HikingCardItem {
  String? name;
  List<dynamic>? images;
  int? length;
  int? rating;
  String? difficulty;

  HikingCardItem(
      {this.name, this.images, this.difficulty, this.length, this.rating});
}
